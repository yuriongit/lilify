name: Tests Check
inputs:
  working-directory:
    required: true
    type: string

runs:
  using: composite
  steps:
    - name: Tests Check
      working-directory: ${{ inputs.working-directory }}
      shell: bash
      run: bun test
