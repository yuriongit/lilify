name: Setup Bun Environment
inputs:

runs:
    using: composite
    steps:
        - name: Checkout Code
          uses: actions/checkout@v5
        - name: Setup Environment
          uses: oven-sh/setup-bun@v2
          with:
              bun-version: "1.3.14"
